#include <iostream>
using namespace std;



// The stack is a data structure that contains an ordered lists of values and supports the following operations:

// push(val):  inserting a value at the top of the stack
// Amortised Complexity: O(1)

// pop(): remove value at the top of the stack
// Cpmplexity: O(1)

// top(): get value at top of stack
// Complexity: O(1)

// STACK IS CALLED `stack` in cpp 
// 

int main()
{
    return 0;
}